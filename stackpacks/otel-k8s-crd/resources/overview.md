## Open Telemetry Kubernetes CRD StackPack

The **Kubernetes Custom Resources** StackPack discovers Custom Resource Definitions (CRDs) and their Custom Resource (CR) instances in your Kubernetes clusters, creating topology components and relations for each.

This provides visibility into the custom extensions installed in your clusters, including operators, controllers, and platform add-ons.
