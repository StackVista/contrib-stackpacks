# notification-operator Stackpack
The Stackpack for the Notification Operator provides metrics and monitors for the Notification Operator, which is used to manage notifications in SUSE Observability with Kubernetes Custom resources (CRs).

The notification operator's pod must be labeled with `app.kubernetes.io/name=notification-operator` to ensure that the metrics and monitors are applied correctly.
